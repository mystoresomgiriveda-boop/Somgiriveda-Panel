# Gemini AI API Key (Get it from https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=your_gemini_api_key_here

# Firebase Configuration (Can also be set as JSON if needed, but the app uses firebase-applet-config.json by default)
# If you want to use env vars for Firebase, you can add VITE_ prefixed variables here.
